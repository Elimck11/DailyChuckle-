const Header = () => {
  return (
    <header className="main-header">
      <nav>
        <a href="#home">Home</a>
        <a href="#post">Post</a>
        <a href="#profile">Profile</a>
      </nav>
    </header>
  );
};

export default Header;
